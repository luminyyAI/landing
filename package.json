{
  "name": "luminy-landing",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 5173 --bind 127.0.0.1",
    "minify": "esbuild js/site.src.js --bundle --minify --format=iife --outfile=js/site.min.js && esbuild styles/main.css --bundle --minify --outfile=styles/main.min.css"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
